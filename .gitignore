# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# environment — never commit real keys
.env
.env.local
.env.*.local

# editor / os
.vscode/
.idea/
.DS_Store

# claude session history / tooling artifacts
.remember/
.playwright-mcp/
.claude/

# real exam source material (papers + audio) — lives in Supabase, never in git
listening sample/
/reading samples/
/samples/

# logs
*.log

.vercel
.env*

# writing reference material + official criteria (source docs, not app code)
writing example/
Writing criteria multilevel.pdf
Chet tili (multilevel) baholash mezonlari - yangi.pdf

# speaking/writing grading criteria PDFs (same rule: source docs, not app code)
Sample for ai/
Speaking band score/
speaking band criteria/
writing band score/
# the folder on disk really does end in a space; quotes are not special
# in .gitignore, so the space has to be backslash-escaped or the pattern
# matches nothing and a dropped PDF shows up as untracked.
writing\ band\ score\ /

# agent tooling / nested tool repos (not part of the app)
caveman/
.agents/
skills-lock.json
